# templates/tasks/helm_package.yml
parameters:
  - name: chartPath
    default: ""
  - name: destination
    default: ""

steps:
  - task: HelmDeploy@1
    displayName: "Package Helm Chart"
    inputs:
      command: "package"
      chartPath: ${{ parameters.chartPath }}
      destination: ${{ parameters.destination }}
